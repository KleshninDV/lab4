[лаб4 отчёт.txt](https://github.com/user-attachments/files/23839023/4.txt)
Клешнин Дмитрий ИТ-7 Лабораторная №4

================================================================================================

Задание 1. Обобщённые типы

Сравнимое
Создайте ссылочный тип Сравнимое, гарантирующий наличие по данной ссылке метода со
следующими характеристиками:
 Называется “сравнить”
 Принимает объект.
 Тип принимаемого объекта может быть изменен без изменения самого Сравнимого.
 Возвращает целое число

Обобщенный изменяемый массив
Измените решение любой задачи 2.2.1 таким образом, чтобы тип хранимого в массиве значения
задавался во время создания объекта.

Создан интерфейс ComparableCustom<T> с методом int сравнить(T other).
Класс Person реализует интерфейс: сравнение по возрасту.
Обобщённый изменяемый массив
Создан класс MutableArray<T>:
Может хранить объекты любого типа T.
Поддерживает методы add, get, set.
Поддерживает динамическое расширение массива.
Метод toArray(Class<T> clazz) возвращает массив типа T.

Алгоритм решения:
Интерфейс ComparableCustom задаёт контракт сравнения объектов.
Класс Person хранит имя и возраст, сравнивает по возрасту.
Класс MutableArray<T> реализует динамический массив с типом элементов, задаваемым при создании.

Пример работы:
 ЗАДАНИЕ 1: ОБОБЩЕННЫЕ ТИПЫ 
-- ComparableCustom --
Иван (20) сравнивается с Пётр (25): -1

-- MutableArray --
MutableArray: [a, b, c]

================================================================================================

Задание 2. Параметризации

Заполнение списка.
Создайте метод, который принимает список чисел (стандартный или из задачи 3.1.6) и заполняет
его значениями от 1 до 100. Метод должен принимать список с более чем одной параметризацией.

ImmutableValueList
Неизменяемый массив ImmutableValueList.
Конструкторы: массив, список, перечисленные значения.
Методы: get, set (возвращает новый объект), isEmpty, size, toArray.

ListFiller
Метод fillNumbers(List<? super Integer>) заполняет список числами от 1 до 100.

Алгоритм решения:
ImmutableValueList сохраняет неизменяемость — любые изменения создают копию.
ListFiller демонстрирует работу с обобщениями и масками типов.

Пример работы:
 ЗАДАНИЕ 2: ПАРАМЕТРИЗАЦИИ 
-- ImmutableValueList --
ImmutableList: [1, 2, 3]

-- ListFiller --
Filled numbers (первые 5 элементов): [1, 2, 3, 4, 5] ...

================================================================================================

Задание 3. Обобщённые методы

Функция.
Разработайте такой метод, который будет принимать список значений типа T, и объект имеющий
единственный метод apply. Данный метод надо применить к каждому элементу списка, и вернуть
новый список значений типа P, при этом типы T и P могут совпадать, а могут не совпадать.
Используйте разработанный метод следующим образом:
1. Передайте в метод список со значениями:“qwerty”, “asdfg”, “zx”, а получите список чисел,
где каждое число соответствует длине каждой строки.
2. Передайте в метод список со значениями: 1,-3,7, а получите список в котором все
отрицательные числа стали положительными, а положительные остались без изменений
3. Передайте в метод список состоящий из массивов целых чисел, а получите список в
котором будут только максимальные значения каждого из исходных массивов.

Фильтр.
Разработайте такой метод, который будет принимать список значений типа T и объект имеющий
единственный метод test (принимает T и возвращает boolean). Верните новый список типа T, из
которого удалены все значения не прошедшие проверку условием.
Используйте разработанный метод следующим образом:
1. Передайте в метод список со значениями: “qwerty”, “asdfg”, “zx”, и отфильтруйте все
строки имеющие менее трех символов
2. Передайте в метод список со значениями: 1,-3,7, и отфильтруйте все положительные
элементы
3. Передайте в метод список состоящий из массивов целых чисел, а получите список в
котором будут только те массивы, в которых нет ни одного положительного элемента.

Сокращение.
Разработайте такой метод, который будет принимать список значений типа T и способ с помощью
которого список значений можно свести к одному значению типа T, которое и возвращается из
метода.
Используйте разработанный метод следующим образом:
1. Передайте в метод список со значениями: “qwerty”, “asdfg”, “zx”, и сформируйте одну
большую строку, которая состоит из всех строк исходного списка.
2. Передайте в метод список со значениями: 1,-3,7, и верните сумму всех значений исходного
списка.
3. Имеется список, состоящий из списков целых чисел, получите общеe количество
элементов во всех списках. Подсказка: решить задачу можно в одно действие или
последовательно использовать методы из 3.3.1 и 3.3.3.
Далее необходимо изменить разработанный метод таким образом, чтобы данный метод
гарантированно не возвращал null и не выбрасывал ошибок в том случае, если исходный список
пуст.

map
Метод map(List<T>, Apply<T,P>) применяет функцию к каждому элементу списка.
Примеры:
Список строк → длина каждой строки [6, 5, 2].
Список чисел → модуль числа [1, 3, 7].
Список массивов → максимум каждого массива [7, 8, 10].

filter
Метод filter(List<T>, Test<T>) оставляет только элементы, прошедшие проверку.
Примеры:
Строки длиной ≥ 3 → [qwerty, asdfg].
Числа отрицательные → [-3].
Массивы без положительных → [[-1,-2,0]].

reduce
Метод reduce(List<T>, T identity, Reducer<T>) сводит список к одному значению.
Примеры:
Список строк → объединённая строка qwertyasdfyzx.
Список чисел → сумма 5.
Список списков → количество всех элементов 9.

collect / toSet
Метод collect(List<T>, SupplierCollection<C>, Accumulator<C,T>) собирает элементы списка в любую коллекцию.
toSet(List<T>) возвращает уникальные элементы, дубликаты удаляются автоматически.
Пример работы: Unique set: [qwerty, asdfg].

Алгоритм решения:
Используются функциональные интерфейсы: Apply, Test, Reducer, Accumulator, SupplierCollection.
Методы работают с обобщёнными типами, поддерживают автоподстановку типов.

Пример работы:
 ЗАДАНИЕ 3: ОБОБЩЕННЫЕ МЕТОДЫ 
-- Функция map (длина строк) --
Strings: [qwerty, asdfg, zx]
Lengths: [6, 5, 2]

-- Функция map (модуль чисел) --
Numbers: [1, -3, 7]
Absolute values: [1, 3, 7]

-- Функция filter (строки длиной >=3) --
Filtered strings: [qwerty, asdfg]

-- Функция reduce (сумма чисел) --
Sum: 5

-- Функция collect (уникальные элементы в Set) --
Unique set: [qwerty, asdfg]

================================================================================================

<img width="515" height="777" alt="image" src="https://github.com/user-attachments/assets/41f7c39e-5f34-492c-9616-03f6a5e0cef5" />

